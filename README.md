# COMSUBRON ONE Watch — preorder landing page

Live at https://css1.buteranet.com (GitHub Pages + Cloudflare).

Single self-contained `index.html` (images embedded). The live order
count/deadline live in `tracker.json` — edit that file to update the
page; no need to touch the HTML.
